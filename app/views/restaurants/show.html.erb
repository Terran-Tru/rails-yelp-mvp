<h2><%= @restaurant.name %></h2>
<p><%= @restaurant.address %></p>
<p><%= @restaurant.category %></p>
<p><%= @restaurant.phone_number %></p>
<h3>Reviews:</h3>
<% if @restaurant.reviews.any? %>
  <ul>
    <% @restaurant.reviews.each do |review| %>
      <li>
        Rating: <%= review.rating %> / Comentario: <%= review.content %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>Aún no hay reviews para este restaurante.</p>
<% end %>

<%= link_to "Agregar nueva review", new_restaurant_review_path(@restaurant), class: "btn btn-primary mb-2" %>

<%= link_to "volver al home", restaurants_path, class: "btn btn-primary mb-2" %>