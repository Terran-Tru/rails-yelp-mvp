<h1 class="text-left text-info fw-semibold mb-3">
      Nueva Review
</h1>

<%= simple_form_for([@restaurant, @review]) do |f| %>
<%= f.input :content %>
<%= f.input :rating, collection: 1..5, prompt: "Selecciona un rating" %>
<%= f.submit "Submit new" %>
<% end %>

